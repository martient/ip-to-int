name: ip_to_int
description: This package can be used to convert IP address to integer and revert it !
version: 1.0.1
homepage: https://github.com/martient/ip-to-int

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
